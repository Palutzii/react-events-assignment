import React from "react";

const Footer = () => {
    return (
            <footer>
                <p>&copy; {new Date().getFullYear()} Event Booker</p>
            </footer>
    );
};

export default Footer;